# ft_inspection

```
cd $HOME ; git clone https://github.com/abello-r/ft_inspection.git .ft_inspection ; cd .ft_inspection ; echo 'alias api="sh $HOME/.ft_inspection/ft_inspection.sh' >> ~/.zshrc ; source ~/.zshrc
```
